<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ui_translation_label" parent="AppTheme">
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:layout_marginStart">0dp</item>
        <item name="android:layout_marginEnd">0dp</item>
        <item name="android:layout_marginLeft">0dp</item>
        <item name="android:layout_marginRight">0dp</item>

        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>

        <item name="android:textSize">20sp</item>
    </style>

    <style name="ui_height_0dp_weight_match_parent" parent="AppTheme">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginRight">8dp</item>

        <item name="android:textSize">20sp</item>

        <item name="android:layout_height">0dp</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="ui_editable" parent="ui_height_0dp_weight_match_parent">
        <item name="android:inputType">textMultiLine</item>
        <item name="android:editable">true</item>
    </style>

    <style name="ui_label" parent="ui_height_0dp_weight_match_parent">
        <item name="android:editable">false</item>
        <item name="android:clickable">false</item>
    </style>

    <style name="ui_label.Clickable">
        <item name="android:editable">false</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="ui_label.Multiline">
        <item name="android:state_multiline">true</item>
    </style>

    <style name="ui_link" parent="ui_label.Clickable">
        <item name="android:textColor">@color/ui_link</item>
    </style>
    <!-- end -->

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
